from django.db.models.signals import post_save, post_delete
from django.dispatch import receiver

# This file is imported by the apps.py file but doesn't have any signals defined yet.
# When signals are needed, they can be added here.
